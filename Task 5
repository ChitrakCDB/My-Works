//Program to print the table in Task 4 in reverse order

#include<stdio.h>
#define lower 300
#define upper 0
#define step 20

int main()
{
    int fahr;
    for(fahr=lower;fahr>=upper;fahr=fahr-step)
        printf("%3d %6.1f\n",fahr,(5.0/9.0)*(fahr-32));
    return 0;
}
